<article class="object object-{{object.type}}">
	{% if object.data is empty %}
		<h1>Sorry</h1>
		<p>This object has no data</p>
	{% else %}
		<h1>{{ object.data.title }}</h1>
		{{ object.data.content | raw }}
	{% endif %}
</article>
